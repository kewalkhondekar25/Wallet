{
  "name": "bank-webhook",
  "version": "1.0.0",
  "description": "A Bank WebHook Server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx esbuild ./src/index.ts --bundle --platform=node --outfile=dist/index.js",
    "start": "node dist/index.js",
    "dev": "npm run build && npm run start"
  },
  "keywords": [
    "webhook",
    "nodejs",
    "express",
    "ts"
  ],
  "author": "Kewal Khondekar",
  "license": "ISC",
  "dependencies": {
    "@repo/db": "*",
    "@types/express": "^5.0.0",
    "dotenv": "^16.4.5",
    "esbuild": "^0.24.0",
    "express": "^4.21.1",
    "path": "^0.12.7"
  }
}
